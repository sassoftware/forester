Multiple Git Repos from a single source file

Yes another for loop for git.


make 
make install

Config

forester init --branch bar --email foo@bar.com --name Joe Foo --wms --wmsbase http://foo.bar.com --wmspath silo/path --subdir ~/git --forest foo

forester forest --forest foobar
